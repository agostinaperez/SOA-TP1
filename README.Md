#TP 1 Arquitectura Orientada a Servicios

cp .env.example .env
docker-compose up -d
docker-compose logs -f init-topics  # Para ver los topics creados

NO OLVIDARME DE QUE HAY QUE METER LAS INSTRUCCIONES O I